<!--Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark red">
    <a class="navbar-brand" href="#">
        La <strong class="orange-text">Taverne</strong> du <strong class="orange-text">JV</strong> 
    </a>

    <!-- Bouton mobile -->
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarCollapse">
        <ul class="navbar-nav mr-auto">

            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_dashboard') }}">
                    <i class="fas fa-chart-line"></i>
                    Dashboard
                </a>
            </li>

            {% if is_granted('ROLE_AUTHOR') %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_articles_index') }}">
                    <i class="far fa-newspaper"></i>
                    Articles
                </a>
            </li>
            {% endif %}

            {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_topics_index') }}">
                    <i class="far fa-comment-alt"></i>
                    Topics
                </a>
            </li>
            {% endif %}

            {% if is_granted('ROLE_MODERATOR') %}
            <li class="nav-item">
                <div class="dropdown">
                    <a class="nav-link" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fas fa-exclamation-triangle"></i>
                        Modération
                    </a>
                    <div class="dropdown-menu scrollable-menu" aria-labelledby="dropdownMenuLink">
                        <h6 class="dropdown-item">Commentaire des articles</h6>
                        <a class="dropdown-item text-danger font-weight-bold" href="{{ path('admin_comment_article_signaler_index') }}">Signalé</a>
                        <a class="dropdown-item text-warning font-weight-bold" href="{{ path('admin_comment_article_moderer_index') }}">Modéré</a>
                        <div class="dropdown-divider"></div>
                        <h6 class="dropdown-item">Commentaire des topics</h6>
                        <a class="dropdown-item text-danger font-weight-bold" href="{{ path('admin_comment_topic_signaler_index') }}">Signalé</a>
                        <a class="dropdown-item text-warning font-weight-bold" href="{{ path('admin_comment_topic_moderer_index') }}">Modéré</a>
                    </div>
                </div>
            </li>
            {% endif %}

            {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_users_index') }}">
                    <i class="fas fa-users"></i>
                    Utilisateurs
                </a>
            </li>
            {% endif %}

        </ul>

        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a href="{{ path('home') }}" class="nav-link" target="_blank">
                    <i class="fas fa-eye"></i>
                    Voir le site
                </a>
            </li>
            {% if app.user %}
                <li class="nav-item">
                    <div class="dropdown">
                        <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                            {% if app.user.picture == null %}
                                <img src="http://image.jeuxvideo.com/avatar-md/default.jpg" alt="Image de profil par défaut" class="avatar avatar-mini"> 
                            {% else %}
                                <img src="{{ app.user.picture }}" class="avatar avatar-mini" alt="Avatar de {{ app.user.username }}">
                            {% endif %}        
                            {{ app.user.username }}
                        </a>
                            
                        <div class="dropdown-menu" aria-labelledby="accountDropdownLink">
                            <a href="{{ path('admin_account_logout') }}" class="dropdown-item">Déconnexion</a>
                        </div>
                    </div>
                </li>
            {% endif %}      
        </ul>
    </div>
</nav>
<!--/.Navbar -->