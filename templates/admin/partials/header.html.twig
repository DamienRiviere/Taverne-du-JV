<!--Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark red">
    <a class="navbar-brand" href="#">
        <i class="fas fa-beer"></i>
        La <strong class="orange-text">Taverne</strong> du <strong class="orange-text">JV</strong> 
        - Administration
    </a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent-333" aria-controls="navbarSupportedContent-333" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent-333">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="#">
                    <i class="fas fa-chart-line"></i>
                    Dashboard
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_articles_index') }}">
                    <i class="far fa-newspaper"></i>
                    Articles
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">
                    <i class="fas fa-exclamation-triangle"></i>
                    Modération
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_users_index') }}">
                    <i class="fas fa-users"></i>
                    Utilisateurs
                </a>
            </li>
        </ul>

        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a href="{{ path('home') }}" class="nav-link" target="_blank">
                    <i class="fas fa-eye"></i>
                    Voir le site
                </a>
            </li>
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                        {% if app.user.picture == null %}
                            <img src="http://image.jeuxvideo.com/avatar-md/default.jpg" alt="Image de profil par défaut" class="avatar avatar-mini"> 
                        {% else %}
                            <img src="{{ app.user.picture }}" class="avatar avatar-mini" alt="Avatar de {{ app.user.username }}">
                        {% endif %}        
                        {{ app.user.username }}
                    </a>
                        
                    <div class="dropdown-menu" aria-labelledby="accountDropdownLink">
                        <a href="{{ path('admin_account_logout') }}" class="dropdown-item">Déconnexion</a>
                    </div>
                </li>
            {% endif %}      
        </ul>
    </div>
</nav>
<!--/.Navbar -->